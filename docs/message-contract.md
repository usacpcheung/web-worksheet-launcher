# Message Contract (Phase 1)

This document defines the launch contract and postMessage contract between:

- **Parent app** (`parent_prototype/parent.html`)
- **Popup renderer** (`server/worksheet_launcher/render.html`)

## 1) Launch query contract

The popup URL query **must** include:

`?launchId=<id>`

Where:

- `launchId`: one-time launch token generated by parent/backend

Example shape:

`https://<host>/worksheet/render.html?launchId=<id>`

## 2) Parent launch-create contract

Before opening popup to the renderer URL, parent obtains a one-time launch token:

`POST /api/worksheet-launch/create`

Request shape:

```json
{
  "rid": "...",
  "worksheet": { "v": 1, "title": "...", "q": ["..."], "rewrite": true },
  "returnOrigin": "https://parent.example"
}
```

Response must include `launchId` (top-level or nested payload accepted by current prototype).

## 3) Startup consume contract

Before rendering questions, popup calls:

`POST /api/worksheet-launch/consume?launchId=<id>`

Expected success response:

```json
{
  "rid": "...",
  "worksheet": { "v": 1, "title": "...", "q": ["..."], "rewrite": true },
  "returnOrigin": "https://parent.example"
}
```

Renderer handling for `returnOrigin`:

- The renderer parses `returnOrigin` once during initialization using `new URL(...)`.
- Only absolute `http:` / `https:` values are accepted.
- The renderer stores and uses only the normalized `.origin` value as the `postMessage` target origin.
- If `returnOrigin` is malformed or invalid, the renderer shows a visible error, disables **Send Back to Parent**, and does not attempt to send results.

Required error handling states in renderer UI:

- Missing launch (`launchId` absent or malformed)
- Expired launch
- Already consumed launch
- Unauthorized launch

## 4) Worksheet payload schema (`worksheet` from consume response)

```json
{ "v": 1, "title": "string", "q": ["...1-N"], "rewrite": true }
```

Validation rules:

- `v` must equal `1`
- `title` should be a string when present
- `q` must be an array of strings with length `1..N` (`N >= 1`)
- `rewrite` should be a boolean when present

## 5) Popup → parent message schema

```json
{
  "type": "worksheetResult",
  "rid": "...",
  "worksheet": { "v": 1, "title": "...", "q": ["..."] },
  "answers": [
    { "index": 0, "question": "...", "answer": "...", "raw": "...", "rewritten": "..." }
  ],
  "meta": { "sentAt": "ISO-8601" }
}
```

Notes:

- `type` is fixed to `worksheetResult`
- `rid` must echo the consumed launch `rid`
- `answers[index]` and `answers[].index` must both map to the same question order as `worksheet.q`
- `answer` is the canonical value for each question; `raw` and `rewritten` are echoed for compatibility with existing parent mapping logic
- `meta.sentAt` is an ISO-8601 timestamp string

## 6) Parent validation requirements (must enforce)

On `message` events, parent must reject payloads unless **all** pass:

1. `event.origin` exactly matches the trusted renderer origin
2. `event.data.type === "worksheetResult"`
3. `event.data.rid` equals the outstanding request id (`rid`) used to launch popup
4. `event.source` is the same popup window reference returned by `window.open(...)`

If any check fails, ignore message and do not apply results.

## 7) One-shot / anti-replay expectation

Launch consume is one-shot. After a launch is consumed, subsequent consume attempts for the
same `launchId` must fail as consumed.

After parent accepts and applies one valid `worksheetResult` for a launch `rid`, that launch
context must be treated as consumed. Parent should clear (or mark consumed) the outstanding
launch context so any subsequent message for that `rid` is rejected and has no effect.

## 8) Phase boundary

Phase 1 only defines contracts/scaffolding.
No new runtime behavior is introduced by this document.
